server:
  port: 8888
spring:
  application:
    name: job-portal-config-server
  cloud:
    config:
      server:
        git:
          uri: ""
          clone-on-start: true
          default-label: master