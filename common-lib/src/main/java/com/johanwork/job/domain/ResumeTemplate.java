package com.johanwork.job.domain;

public enum ResumeTemplate {
    CLASSIC,        // traditional single-column layout
    MODERN,         // two-column with accent color
    CREATIVE,       // graphic-heavy, suited for designers roles
    MINIMAL,        // clean whitespace, no decorations
    PROFESSIONAL   // ATS-optimised, plain formatting
}
